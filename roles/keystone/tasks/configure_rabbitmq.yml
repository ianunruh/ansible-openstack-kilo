- name: ensure rabbitmq vhost
  rabbitmq_vhost:
    name: "{{ keystone_rabbitmq_vhost }}"
    state: present

- name: ensure rabbitmq user
  rabbitmq_user:
    user: "{{ keystone_rabbitmq_user }}"
    password: "{{ keystone_rabbitmq_password }}"
    vhost: "{{ keystone_rabbitmq_vhost }}"
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    state: present
