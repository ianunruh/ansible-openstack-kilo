- name: ensure rabbitmq vhost
  rabbitmq_vhost:
    name: "{{ vhost }}"
    state: present

- name: ensure rabbitmq user
  rabbitmq_user:
    user: "{{ user }}"
    password: "{{ password }}"
    vhost: "{{ vhost }}"
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    state: present
