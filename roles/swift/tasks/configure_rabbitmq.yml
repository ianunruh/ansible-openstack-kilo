- name: ensure rabbitmq vhost
  rabbitmq_vhost:
    name: "{{ swift_rabbitmq_vhost }}"
    state: present

- name: ensure rabbitmq user
  rabbitmq_user:
    user: "{{ swift_rabbitmq_user }}"
    password: "{{ swift_rabbitmq_password }}"
    vhost: "{{ swift_rabbitmq_vhost }}"
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    state: present
