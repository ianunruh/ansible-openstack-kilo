- name: ensure mysql db
  mysql_db:
    name: "{{ nova_mysql_db }}"
    state: present

    login_host: "{{ nova_mysql_host }}"
    login_user: root
    login_password: "{{ nova_mysql_login_password }}"

- name: ensure mysql user
  mysql_user:
    name: "{{ nova_mysql_user }}"
    host: "%"
    password: "{{ nova_mysql_password }}"
    priv: "{{ nova_mysql_db }}.*:ALL"
    state: present

    login_host: "{{ nova_mysql_host }}"
    login_user: root
    login_password: "{{ nova_mysql_login_password }}"
