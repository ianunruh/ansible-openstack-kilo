- name: ensure mysql db
  mysql_db:
    name: "{{ neutron_mysql_db }}"
    state: present

    login_host: "{{ neutron_mysql_host }}"
    login_user: root
    login_password: "{{ neutron_mysql_login_password }}"

- name: ensure mysql user
  mysql_user:
    name: "{{ neutron_mysql_user }}"
    host: "%"
    password: "{{ neutron_mysql_password }}"
    priv: "{{ neutron_mysql_db }}.*:ALL"
    state: present

    login_host: "{{ neutron_mysql_host }}"
    login_user: root
    login_password: "{{ neutron_mysql_login_password }}"
